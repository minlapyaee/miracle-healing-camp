import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#4B4D99",
            secondary: "#36B6F9"
        },
        darkColor: "#000000",
        whiteColor: "#ffffff",
        secondary: {
            main:  "#f9c937"
        }
    }
});

export { theme }